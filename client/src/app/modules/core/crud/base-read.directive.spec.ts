import { BaseReadDirective } from './base-read.directive';

describe('BaseReadDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseReadDirective();
    expect(directive).toBeTruthy();
  });
});
